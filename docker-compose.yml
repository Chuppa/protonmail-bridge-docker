version: "3.8"

services:
  protonmail-bridge:
    image: ghcr.io/YOUR_USER/protonmail-bridge-docker:latest
    container_name: protonmail-bridge
    restart: unless-stopped
    ports:
      - "1025:25/tcp"
      - "1143:143/tcp"
    volumes:
      - ./config:/root/.config/protonmail
